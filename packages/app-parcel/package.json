{
  "name": "@ecma-modules/app-parcel",
  "version": "0.1.0",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "parcel build index.html",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@ecma-modules/lib-date": "0.1.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2"
  }
}
